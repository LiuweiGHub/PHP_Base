## 自我介绍

——————————————————————————————————     	

名字+工作单位+项目开发+很荣幸来到贵公司面试【不必要暴漏太多信息，不说上家公司不好】

对加班的看法？

你还有什么想了解的？  培训\福利\出差\工作开发模式

第一天见领导、装环境、控制器、找项目组长领取账号密码获取代码、项目测试服务器、链接数据库、本地代码先跑起来【u盘考取加班加点了解项目】

问到不会的可以反问。--学习心态

## 基础问题相关

------

1、面向对象的理解？ 

```html
<!--标准答案-->
<!--面向对象是一种编程思想，它更加符合人的编程逻辑，「举例说明与面相过程的区别？小明、小红吃饭、睡觉、打豆豆」,面向对象封装、继承、多态三种特征，但是PHP不支持多态，但是可以通过接口实现。-->
```

2、面向对象里的权限限定符

```Php
private  //私有的属性和方法，子类中和类外都不能调用
public   //可以在类外调用
protected//受保护的，可以在子类中调用  
```

3、抽象类和接口的区别？！！！

```php
//概念
抽象类：
接  口：只包含抽象方法的特殊抽象类。换句话说，接口类中不允许存在普通方法和属性
//抽象类与接口的区别  
  1、有抽象方法的类叫做抽象类，抽象类中不一定有抽象方法，抽象方法必须使用 abstract 关键字定义
  2、接口中全部是抽象方法,方法不用使用 abstract 定义 
  3、当多个同类的类要设计一个上层，通常设计为抽象类，当多个异构的类要设计一个上层，通常设计为接口
```

```php
//抽象类example

```

4、php怎么实么现多个接口？——因为PHP面向对象，不具有多态性

5、session与cookie的区别？如何实现session数据共享？禁用cookie后如何使用session？

session共享：多台服务器，硬盘存在临时文件。

方式一：网络磁盘挂载,磁盘网络共享，session重写，网盘服务

方式二：session入库，mysql、memcache、redis 【tp框架有接口】  ———推荐、速度快

方式三：使用请求分发使同一用户只访问同一台服务器【不共享】。  ip_hash  

session_id拿到即可 

6、ajax的原理？项目开发中哪些地方用到了ajax？

先实例化一个ajax「XMLHtttpRequest」对象，通过对象http请求，数据格式：xml或者json

javascript+xml 后期支持json

同步异步：同步=阻塞【排队】  异步=并行操作【非阻塞】 

页面是否跳转为标准，而不是是否刷新

ajax缺点：页面结构不变，导致数据是假的,SEO不好，会导致缓存所以get请求后加一个+随机数或时间戳，避免时间戳

分页、 表单、一切发送请求都可以

7、如何理解命名空间？

解决【自动加载】时命名冲突的问题，5.3以上开始支持，TP支持5.3以上就是因为命名空间

8、Mysql存储引擎有哪些？区别有什么？项目中怎么选择？

六种！myisan

Pv、uv 【page visitor /unique visitor】

并发：QPS：数据读写吞吐量     几万/s ———减轻并法思路：命中率／规定最大数据量

思路：加大带宽

斗鱼：300万/月   带宽费用,登录才能看高清【增强用户数，节省带宽】未登录时也可以规定播放时长

pp鸟哥，新浪架构师

9、简述事务的处理流程？哪些地方用到事务？一条sql语句需要使用事务吗？

事务要么成功要么失败，多个操作，为了数据的完整型

订单支付、银行转账

一条sql不用事务

10、sql五子句顺序？

Where->groupby->having->orderly->limit

where只能查询真实字段

having可以查询聚合函数查询之后的虚拟字段，通常与groupby配合使用

11、简述单例模式？为什么使用static？三私一公

12、mysql索引的种类有哪些？为什么需要用到索引？实际开发中如何使用索引？索引的底层怎么实现？

主键、唯一、普通索引（全文、复合）

索引是存在硬盘中，先查找常用的字段，存储物理地址（myisam），存储数据不排序

innodb（主键直接对应数据） 存入数据默认排序，因为记录行号

13、使用索引对数据库的影响？

以空间换时间！不要轻易设置索引

14、memcache实际项目中怎么使用的？

15、redis有哪些数据类型？实际开发中使用？与memcache的区别？

string  

数据类型

存储大小

数据持久化

— redis如何实现持久化？ 		AOF和快照

— 秒杀、抢购。 队列【原子性】：先进先出

— 计时器

【提交订单使用redis的问题？文件锁？】

##16.如何审查代码质量？都有哪些规范？《代码质量规范》







## 项目相关问题

1、支付是怎么实现的？由于服务器断电，但是客户已经付款导致下单失败怎么办？

2、简述商品管理模块？有哪些数据表？

> 商品表怎么建的？包含哪些字段？业务逻辑？用到哪些技术和相关插件？实现了什么效果？

3、RBAC实现原理？通过几张数据表实现？

> 用户—角色—节点（权限）

4、简述无限极分类的原理

采用递归思想

pid

5、实现递归调用文件夹中的子文件

6、简述购物车的实现逻辑

7、当购物车商品支付失败，如何操作？

可在数据表中加一个订单状态的字段（已支付、已下单）,判断这个字段，避免下单成功但是支付未成功的问题

8、如何做定时任务的？

方式一：前端 settimeout（）

方式二：后端死循环+sleep（）

方式三：Linux  crontab???? 定时请求脚本。【应用： 数据库定时备份】

9、课程信息管理模块？数据表有哪些？如果老师有课则提醒该老师上课？

10、开发中遇到什么难点？怎么解决的？

11、项目在本地测试没问题，部署到服务器上出问题了，怎么办？

> 找到log目录，打印错误日志，分析



socket可以解决跨域问题

# 全栈面试

## 1、ajax概述「非阻塞，用户体验」「原生ajax XMLHttpRequest  IE浏览器解决兼容问题」

## 2、为什么使用jQuery？200k  

## 3、ajax使用get和post请求哪个多？数据缓存的问题？加随机时间戳的标准是什么？「键如何命名—大公司加一个版本号」

## 4、跨域问题   jsonp／curs／window.name/post.message/反向代理服务器／socket		 script、img、iframe「sendboxs属性添加后的解决办法」

## 5、nodejs的了解？koa2—v

## 6、ES6的了解？「官方文档」

let「块级作用域，不能先使用再定义」  const   箭头函数「本身this undefined，没有this说法」 

promise状态更改	「异步请求，可读性好」 直接then()调用result  ，catch()直接调用

ES7以同步协议书写异步

## 7、七牛云用的带宽是多少？怎么评价一个网站大小？「PV  UV」

## 8、索引「主键索引、唯一键、普通索引」

## 9、缓存技术？memcache、redis「抢购、商品排名、相同朋友{set、hash}」

## 10、《尽在双十一》《淘宝这十年》 前后端技术  	

## 前端「渐近增强  优雅降级」 vue、react、angular IE10之前不考虑

适应性、第一版开发不考虑其他浏览器兼容

第二版后打补丁

## 11、课程考试试题怎么管理的？

按分值字段抽取，抽取几率相同吗？如果按错题率高、低抽取怎么实现？——加字段 错题次数

考试完毕后如何与答案进行校验？四个选项怎么排列？死的！灵活的怎么实现？

##12、下载文件如何实现不乱码？强制转码有什么问题？怎么把查取的数据写入excel？ “PHPexcel扩展”   “echarts”     ——最好写入的时候就是GBK

## 13、购物车未登录存cookie（最多200） localstorage、sessionstorage 

## 14、运费怎么算？易碎品、奢侈品、。。。。

## 15、OA系统坑

## 16、H5中storage最多可以存多少？ 5M  关于视频、音频，各支持什么格式的播放？H5文档：video 支持 「mp4   ogg  webm」 没有一个浏览器支持以上三种，视频总长度怎么获取？current_time  ；初始音量。。。

视频兼容 360、qq内核同IE，考虑兼容问题 ；

 ck_player插件使用flash

swiper.js实现轮播

Safari不用flash。「耗电、耗费资源、2020年后不再更新安全问题」

## 17、tp、laravel等框架的日志系统怎么记录错误日志的？正式部署一般自定义错误日志

慕课网----高性能日志   &&  PHP开发接口

nodejs----了解log4日志

PHP错误级别----error_reporting    

- 错误级别 
- 报错时间

## 18、移动端电话号码做成链接，点击后拨出电话？ href=“tel:XXXXXXXXXX”

## 19、调用摄像头

## 20、使用css3动画优先使用过渡 transform ，为什么？

## 21、vuex了解吗？ 父子组件通信？ 父给子通过属性、子给父通过事件

标准的传递方式（非父子组件）：$on「接收事件」 $emit「触发事件」只能存在入口函数根组件

vue脚手架

## 22、如何提高组件的复用性？ 只要使用v-for、  需设置一个唯一key，进行标识组件

## 23、keep_alive

## 24、路由如何去掉访问地址中的#号？ Vue_router  配置文件中 model 设置为history

## 25、<router-link>默认样式如何去掉？ 单文件组件 style 可加一个？？？属性

## 26、vue中命名路由？八大钩子函数？第三方 axios、mui、adminUI...怎么引入组件中使用？

## 26、Object.assign( ) 

## 27、ES6创建一个类  class  继承 extends

阮一峰网络日志 js继承

## 28、闭包

一个函数返回一个函数

这个函数使用了外边函数的变量

## 29、实现二维码 使用PHP

QR_code 第三方类库

## 30、单点登录怎么实现？  u_center插件，可用于第三方登录

## 31、三级分销？ 

## 32、积分商城

## 33、MySQL优化

- sql

- 分区分表

- 索引

  ​





脉脉

## 34vue与react的区别

Vue的写法相对简洁，React写法相对灵活

Vue有双向数据绑定，React没有（是单向的）

## 35、Vue与Angular的区别

他俩都可以实现双向数据绑定

Vue原理:Object.defineproperty({

Get:

Set:

})



angular实际上没有办法监视数据有没有变化

angular要调用$applar()

脏检测

{{name}}.    angular中就会有一个watch方法专门用来更新这个name

​			这个wanth方法会对比dom中的name和$scope中的name,如果不一样就更新，提升angular性能

{{age} }  angular中就会再有一个watch方法专门用来更新这个age

……1000个

这些所有的watch方法组成一个数组watchers[].length  ===1000



“给人感觉性能较差，所以叫做脏检测「脏检查」”

“数据调研----只用当页面绑定了超过2000个数据时，才会明显感觉性能变差”



每当我们调用$apply时，angular内部就遍历这个依次调用这里的watch方法来更新dom



从dom中将数据更新到js，就一种方式(事件监听)

setInterval(function({

每一秒，对比数据与dom的区别，不一样就更新

….

….

}),1000) 浏览器会尽快执行这个function(){}  js执行优先级

for()



while循环，一直循环判断数据有没有变化

while(true){

if(数据变化){

​	更新dom

}

}





js是单线程

《我是一个线程》 初生牛犊

CPU同时只能做一件事

看电影，聊天，CPU这一毫秒放电影，下一毫秒聊天(接收聊天信息),这个速度非常快



